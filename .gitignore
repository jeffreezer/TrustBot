# --- Confidential third-party reference material: NEVER commit ---
# Real vendor documents kept locally as references only (move out of the folder when done).
# Anchored to the repo root so they ignore the top-level confidential folders WITHOUT also
# matching nested paths like deploy/gcp/ (git is case-insensitive on macOS).
/OpenAI/
/GCP/
Illustrative*SOC2*.pdf
*SOC2*report*.pdf

# Env / secrets
.env
*.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node / Next.js
node_modules/
.next/

# Local storage / data
_storage/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local agent / tooling state (session transcripts, memory — never commit)
.claude/
