fastapi==0.115.*
uvicorn[standard]==0.30.*
sqlalchemy==2.0.*
psycopg[binary]==3.2.*
pgvector==0.3.*
alembic==1.14.*
pydantic-settings==2.*
boto3==1.35.*
openpyxl==3.1.*
# Phase 2 embedding (local BGE-M3, CPU). torch is installed separately in the
# Dockerfile from the CPU-only index to avoid pulling the multi-GB CUDA build.
sentence-transformers==3.*
