# Heavy model libs (torch CPU, sentence-transformers) live in their own file so the
# Docker model-bake layer caches independently of these fast-changing app deps.
-r requirements-models.txt
fastapi==0.115.*
uvicorn[standard]==0.30.*
sqlalchemy==2.0.*
psycopg[binary]==3.2.*
pgvector==0.3.*
alembic==1.14.*
pydantic-settings==2.*
boto3==1.35.*
# Google Cloud Storage backend (STORAGE_BACKEND=gcs); lazily imported only when selected.
google-cloud-storage==2.*
openpyxl==3.1.*
# Reads the YAML golden set in the Phase 4 eval harness (evals/run_evals.py).
pyyaml==6.*
